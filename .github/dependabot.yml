# T-162 — track llama-cpp-python (transitive diskcache carrier) for upstream cache changes.
version: 2
updates:
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    allow:
      - dependency-name: llama-cpp-python
    open-pull-requests-limit: 1
    labels:
      - dependencies
      - security
